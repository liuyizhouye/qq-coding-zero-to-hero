# 可选配置：当前项目默认离线 mock，以下变量仅用于未来扩展到真实 API 时参考。

OPENAI_API_KEY=sk-xxxx
OPENAI_MODEL=gpt-5

# Optional for MCP extensions:
MCP_SERVER_URL=https://example-mcp-server.local/mcp
MCP_ALLOWED_TOOLS=ask_question,read_wiki_structure
